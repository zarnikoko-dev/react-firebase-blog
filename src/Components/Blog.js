import React from 'react'
import { withRouter } from 'react-router-dom'

const Blog = (props)=> {
    return(
        <div>
            <h3>This is single blog</h3>
        </div>
    )
}

export default withRouter(Blog)