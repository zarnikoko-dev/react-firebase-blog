import React from 'react'
import { withRouter } from 'react-router-dom'

const ShowBlogs = (props)=> {
    return(
        <div>
            <h3>Blogs will show here</h3>
        </div>
    )
}

export default withRouter(ShowBlogs)